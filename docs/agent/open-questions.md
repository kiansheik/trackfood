# Open Questions

- Whether the production GitHub Pages site will use repository-path hosting or a custom domain root. `BASE_PATH` and the `PAGES_BASE_PATH` repository variable support both.
- Whether future versions should persist nutrition-label images. The first version stores OCR text and metadata only.
- Which adaptive TDEE approach should be used later. The current implementation keeps metabolism calculations isolated and manually overridable.
- Whether meal names should become first-class editable records rather than settings strings.
